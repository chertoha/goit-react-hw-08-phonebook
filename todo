- chakra ui styles:

  - Login - (main page) + link to reg-page
  - Reg page - (main page) + link to login-page
  - Contacts page- (secondary page) + profile menu + button "+" to add new
    contact - profile
  - Profile menu - chakra dropdown (menu name = username, dropdown= email and
    logout button)
  -

- pagination ?
- component Profile - errors if no user = undefined

- auth errors check if it works - LoginForm, RegistrationForm, App(refreshing),
  App (Logout) / Status codes check!
