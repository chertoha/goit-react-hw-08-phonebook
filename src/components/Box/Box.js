import styled from 'styled-components';
import {
  color,
  layout,
  space,
  flexbox,
  typography,
  border,
} from 'styled-system';

const Box = styled('div')(color, layout, space, flexbox, typography, border);

export default Box;
